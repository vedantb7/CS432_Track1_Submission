You are a senior backend QA and reliability engineer. Upgrade this project so ACID validation is complete at database layer and API/application layer, and verify all required functionality with reproducible evidence.

Goal
Ensure the system correctly implements and demonstrates:

Atomicity
Consistency
Isolation
Durability
And explicitly covers:
Race condition testing
Concurrent user testing
Stress testing
Failure simulation and recovery
Required outcomes

Add true API-level concurrency tests (not only direct DB manager calls).
Keep existing DB-level ACID tests and improve where needed.
Add stress testing using a proper load-testing tool and document which tool is used.
Produce pass/fail evidence artifacts and a clear final report.
Implementation requirements

API-level tests must issue real HTTP requests against critical transactional endpoints.
Include concurrent checkout/order/payment-like flows that can conflict on shared resources.
Include race tests where multiple clients update the same logical entity.
Include concurrent user tests with configurable virtual users and ramp-up.
Include stress tests with increasing load stages and measurable throughput/latency/error rate.
Include failure simulations:
a. Mid-transaction exception injection
b. Rollback verification
c. Process restart/crash-recovery validation for committed data
Validate ACID assertions after each test, not just response codes.
Stress testing tool requirement

Use one explicit tool (recommended: Locust).
Add runnable load scripts with scenario definitions and thresholds.
Report p50/p95/p99 latency, RPS/throughput, and error percentage.
Define pass criteria for each load stage.
Deliverables

Updated automated test suite for DB-level ACID and API-level ACID.
Load-testing scripts and config.
One command (or script) to run all validations end-to-end.
Machine-readable results (JSON/CSV) and a human-readable evidence report.
A traceability matrix mapping each requirement to:
a. Test name
b. How to run it
c. Expected result
d. Actual result
Acceptance criteria

Race condition test exists and passes with deterministic assertions.
Concurrent user test exists and passes with configurable user count.
Stress test exists, uses the declared tool, and outputs throughput + latency metrics.
Failure simulation test exists and proves rollback + recovery behavior.
API/application-level ACID tests are present and passing.
Documentation is updated so another engineer can reproduce all results from scratch.
Execution instructions

First inspect current tests and identify gaps.
Implement missing tests and scripts.
Run everything and fix failures.
Return:
a. What was added/changed
b. Exact run commands
c. Test summary table
d. Remaining risks or limitations
Quality bar
Do not stop at scaffolding. Complete implementation, execute tests, and provide final evidence that all required functionalities are actually verified.
